node {
   def mvnHome
   stage('Preparation') { // for display purposes
     echo "in stage build"
   }
   stage('Build') {
      // Run the maven build
      fileExists 'deployment.yaml'
      echo "in stage build"
   }
   stage('Results') {
      echo "in stage build"
   }
}
