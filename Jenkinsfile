node {
   def mvnHome
   stage('Preparation') { // for display purposes
     echo "in stage build"
   }
   stage('Build') {
      // Run the maven build
      echo "in stage build"
   }
   stage('Results') {
      echo "in stage build"
   }
}
